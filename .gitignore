node_modules/
.next/
out/
dist/
.env
.env.*
!.env.example
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Provided dataset bundle is large and should be downloaded/provided separately.
# Brackets are escaped so the literal folder name is matched (not a glob class).
\[PUB\] India_runs_data_and_ai_challenge/
__MACOSX/

# Stray screenshots dropped into the repo root.
Screenshot*.png

# Local Claude Code tooling config.
.claude/

# Stray / non-code files kept local, out of the repo.
logo.png.png
file1.txt.txt
file2.txt.txt
scratch_docs.txt
suggest.txt
*.pptx

# Temporary smoke outputs.
outputs/*smoke*
outputs/speed_*
dev-server*.log
dev-server*.log
